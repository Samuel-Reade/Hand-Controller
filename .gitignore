# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
# shared: pins the editor to the project's TypeScript (see the file's comment)
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
node_modules.nosync

# macOS duplicate folder
node_modules 2

# Playwright MCP screenshots, snapshots and console logs
.playwright-mcp

# Verification screenshots: the ones PORT_LOG.md cites are committed; new ones stay local
shots/

# MediaPipe wasm is served from node_modules by vite.config.ts; never vendor it
public/mediapipe/
